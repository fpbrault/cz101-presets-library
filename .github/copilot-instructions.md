# Copilot Instructions

- Use `bun` as the package manager in this repository.
- When writing or modifying temporary code, include an explicit `TODO:` comment at the exact temporary location so it can be tracked and removed later.
