{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Capability for the main synth window and the settings popup window",
	"windows": ["main", "settings"],
	"permissions": [
		"core:default",
		"midi:default"
	]
}
