[workspace]
members = [
    "tauri-app",
    "cz-synth",
    # cz-synth-vst added in Phase 3 (requires nih-plug git dep)
    # "cz-synth-vst",
]
resolver = "2"

[profile.release]
lto = true
strip = "symbols"
